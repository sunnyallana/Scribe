# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
out/
.next/
.turbo/
.vite/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
lerna-debug.log*

# Env
.env
.env.local
.env.*.local
!.env.example

# Editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.recommended.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Testing / coverage
coverage/
.nyc_output/
*.lcov

# Caches
*.tsbuildinfo
.eslintcache
.prettiercache
.cache/

# Tauri
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/
# Updater signing private key. `.pub` is safe to commit (it's the
# public key referenced from tauri.conf.json's `plugins.updater.pubkey`);
# the un-suffixed file is the secret and must stay local.
apps/desktop/src-tauri/.updater-key
# Bundled tectonic binaries — too large to track, copied locally
# before `tauri build` from `~/scribe-tools/tectonic-<ver>/` or
# downloaded by `scripts/setup.{sh,ps1}`. The directory itself is
# preserved via `.gitkeep`.
apps/desktop/src-tauri/resources/tectonic
apps/desktop/src-tauri/resources/tectonic.exe

# Supabase local stack volumes
supabase/.branches/
supabase/.temp/

# Generated artifacts
**/*.generated.ts
**/db.generated.ts

# Temporary
tmp/
*.tmp

# Rust
servers/rust/target/
**/*.rs.bk
**/Cargo.lock.bak

# Local runtime artifacts that shouldn't be tracked.
# `dump.rdb` is Redis's RDB snapshot, `redis.pid` is the daemon PID
# file, `runtime.*.log` are server stdout/stderr captures, and the
# `recover-from-yjs.mjs` / `yjs-pingpong-test.mjs` files are throwaway
# debug scripts that landed in the repo during a debug session.
dump.rdb
servers/rust/redis.pid
servers/rust/runtime.out.log
servers/rust/runtime.err.log
servers/rust/redis.out.log
servers/rust/redis.err.log
apps/web/dev.out.log
apps/web/dev.err.log

# Scribe runtime config — copy of `scribe.config.toml.example`.
# Tracked is fine but most devs keep theirs gitignored.
scribe.config.toml
